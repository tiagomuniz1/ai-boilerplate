{
  "name": "project-root",
  "engines": {
    "node": ">=24.0.0 <25.0.0"
  },
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*",
    "tools/*"
  ],
  "scripts": {
    "dev": "yarn workspace @app/shared build && concurrently \"yarn workspace @app/frontend dev\" \"yarn workspace @app/backend dev\"",
    "build": "yarn workspace @app/frontend build && yarn workspace @app/backend build",
    "test": "yarn workspace @app/frontend test && yarn workspace @app/backend test",
    "ai": "yarn workspace @app/ai-cli ai"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "ts-node": "^10.9.2"
  },
  "packageManager": "yarn@4.14.0"
}